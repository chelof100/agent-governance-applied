# -*- coding: utf-8 -*-
"""
Principal Set P — registry of identity-bound human principals (P8 §3).

Per P7 §12 (def): P = {H_1, ..., H_n} is a pre-registered set of
identity-bound principals whose re-authorization decisions are
admissible at the governance layer.

DC.1 implication: P is read-only at the execution layer. Modifications
are governance events themselves (RECALIBRATE_PRINCIPAL_SET APB).

Sprint 1 scaffolding — implementation pending.
"""
raise NotImplementedError("agent/principal.py — implement in Sprint 1")
